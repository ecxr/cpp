/*
 * Util: utility functions for a weighted undirectional graph
 *
 * Author: Sky King (skyking@gmail.com)
 *
 */

#include <cstdlib>

// Returns a random integer between 0 and 1.
double prob()
{
    return (static_cast<double>(rand()) / RAND_MAX);
}
